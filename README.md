# Terracore Website

This repository contains a multi-page React website for Terracore, a New Zealand based 4WD parts, gear, and lifestyle brand.

## Setup

```
npm install
npm run dev
```

The project uses Vite and Tailwind CSS. Production build is available via `npm run build`.
